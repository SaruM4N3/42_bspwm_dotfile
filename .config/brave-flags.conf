# Brave flags — 42-school / junest environment
# Host kernel DRM 3.42 < mesa-tkg-git requirement (3.54).
# GPU process fails DRI init → Chromium retries constantly, causing slowness.
# --disable-gpu forces SwiftShader from the start (faster than the error path).
--disable-gpu
--no-sandbox